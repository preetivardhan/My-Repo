Set JAVA home on RHEL

export JAVA_HOME=/opt/jdk/jdk-11.0.3
export PATH="$JAVA_HOME/bin:$PATH"

Open JDK (yum java package) installs JDK by default in /usr/lib/jvm/java-1.8.0-openjdk/  -- {So set this path in export JAVA_HOME}
