#####If Ubuntu GUI doesn't load (not because of the password issue) 
apt update
apt upgrade
apt-get install --reinstall ubuntu-desktop
reboot

##############screens######
Helps in running any long script in detached console 
Ctrl+A and then D to come out of the screen
screen -ls                    to list the available screens
screen -r <id>                to attach to screen
